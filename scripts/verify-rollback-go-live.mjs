#!/usr/bin/env node
import{execFileSync}from"node:child_process";const [repo,name]=process.argv.slice(2);const env=JSON.parse(execFileSync("gh",["api",`repos/${repo}/environments/${name}`],{encoding:"utf8"}));const reviewers=(env.protection_rules??[]).find(x=>x.type==="required_reviewers")?.reviewers??[],policy=env.deployment_branch_policy;if(reviewers.length<1||!policy||(policy.protected_branches!==true&&policy.custom_branch_policies!==true))throw new Error("environment lacks reviewers or restrictive branch policy");
