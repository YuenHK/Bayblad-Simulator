#!/usr/bin/env node
import{execFileSync}from"node:child_process";const [repo,name]=process.argv.slice(2);const env=JSON.parse(execFileSync("gh",["api",`repos/${repo}/environments/${name}`],{encoding:"utf8"}));const reviewers=(env.protection_rules??[]).find(x=>x.type==="required_reviewers")?.reviewers??[];if(reviewers.length<1||!env.deployment_branch_policy)throw new Error("rollback approval environment lacks reviewers or branch policy");
