import fs from "node:fs";import path from "node:path";import crypto from "node:crypto";import {execFileSync} from "node:child_process";
const [rootArg,allowedSigners,repositoryId,repositoryName,currentHead,currentBundle,rootKeySha,highGeneration,highDigest]=process.argv.slice(2),root=fs.realpathSync(rootArg),directory=path.join(root,".production-policy-ledger"),hex=n=>new RegExp(`^[a-f0-9]{${n}}$`);
if(!/^\d+$/.test(repositoryId)||!/^[-A-Za-z0-9_.]+\/[-A-Za-z0-9_.]+$/.test(repositoryName)||!hex(40).test(currentHead)||!hex(64).test(currentBundle)||!hex(64).test(rootKeySha)||!/^\d+$/.test(highGeneration)||!hex(64).test(highDigest))throw new Error("invalid ledger binding");
if(crypto.createHash("sha256").update(fs.readFileSync(allowedSigners)).digest("hex")!==rootKeySha)throw new Error("root allowed-signers fingerprint mismatch");
const canonical=entry=>JSON.stringify({schemaVersion:1,purpose:"production-policy-root-rotation",generation:entry.generation,previousReceiptDigest:entry.previousReceiptDigest,repositoryId:entry.repositoryId,repositoryName:entry.repositoryName,policyCommit:entry.policyCommit,policyTreeDigest:entry.policyTreeDigest,bundleSha256:entry.bundleSha256,createdAt:entry.createdAt,signerKeyId:entry.signerKeyId})+"\n";
const names=fs.readdirSync(directory).sort(),jsonNames=names.filter(name=>/^\d{6}\.json$/.test(name));if(!jsonNames.length||names.length!==jsonNames.length*2)throw new Error("ledger files must be exact pairs");let previous="0".repeat(64),latest;
for(let index=0;index<jsonNames.length;index++){const generation=index+1,name=`${String(generation).padStart(6,"0")}.json`;if(jsonNames[index]!==name||!names.includes(`${name}.sig`))throw new Error("ledger generation gap");const file=path.join(directory,name),signature=`${file}.sig`,bytes=fs.readFileSync(file),text=bytes.toString("utf8");if((text.match(/"(?:[^"\\]|\\.)*"\s*:/g)||[]).length!==11)throw new Error("duplicate or unexpected JSON keys");const entry=JSON.parse(text);if(canonical(entry)!==text||entry.generation!==generation||entry.previousReceiptDigest!==previous||entry.repositoryId!==repositoryId||entry.purpose!=="production-policy-root-rotation"||!hex(40).test(entry.policyCommit)||!hex(64).test(entry.policyTreeDigest)||entry.policyTreeDigest!==entry.bundleSha256||!hex(64).test(entry.bundleSha256)||!/^[-A-Za-z0-9_.]+$/.test(entry.signerKeyId)||!/^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\dZ$/.test(entry.createdAt))throw new Error("ledger schema or chain mismatch");execFileSync("/usr/bin/git",["-C",root,"merge-base","--is-ancestor",entry.policyCommit,currentHead]);execFileSync("/usr/bin/ssh-keygen",["-Y","verify","-f",allowedSigners,"-I",entry.signerKeyId,"-n","steam-top-production-policy-root","-s",signature],{input:bytes,stdio:["pipe","ignore","pipe"]});previous=crypto.createHash("sha256").update(bytes).digest("hex");latest=entry}
if(String(latest.generation)!==highGeneration||previous!==highDigest||latest.bundleSha256!==currentBundle)throw new Error("ledger high-water or current policy mismatch");process.stdout.write(JSON.stringify({schemaVersion:1,purpose:"verified-production-policy-ledger",latest,latestReceiptDigest:previous})+"\n");
