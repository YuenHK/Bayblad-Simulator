import fs from "node:fs";
const text=fs.readFileSync(process.argv[2],"utf8"),value=JSON.parse(text),hex=(x,n)=>typeof x==="string"&&new RegExp(`^[a-f0-9]{${n}}$`).test(x);
const canonical=JSON.stringify({schemaVersion:1,purpose:"production-policy-root-rotation",generation:value.generation,previousReceiptDigest:value.previousReceiptDigest,repositoryId:value.repositoryId,repositoryName:value.repositoryName,policyCommit:value.policyCommit,policyTreeOid:value.policyTreeOid,bundleSha256:value.bundleSha256,anchorSha256:value.anchorSha256,anchorGeneration:value.anchorGeneration,createdAt:value.createdAt,signerKeyId:value.signerKeyId})+"\n";
const validTime=typeof value.createdAt==="string"&&/^\d{4}-\d\d-\d\dT\d\d:\d\d:\d\d\.\d{3}Z$/.test(value.createdAt)&&new Date(value.createdAt).toISOString()===value.createdAt;
if(text!==canonical||value.schemaVersion!==1||value.purpose!=="production-policy-root-rotation"||!Number.isSafeInteger(value.generation)||value.generation<1||!hex(value.previousReceiptDigest,64)||!/^\d+$/.test(value.repositoryId)||!/^[-A-Za-z0-9_.]+\/[-A-Za-z0-9_.]+$/.test(value.repositoryName)||value.repositoryName.normalize("NFC")!==value.repositoryName||!hex(value.policyCommit,40)||!hex(value.policyTreeOid,40)||!hex(value.bundleSha256,64)||!hex(value.anchorSha256,64)||!Number.isSafeInteger(value.anchorGeneration)||value.anchorGeneration<1||!validTime||!/^[-A-Za-z0-9_.]+$/.test(value.signerKeyId))throw new Error("invalid canonical production policy entry");
