import { createHash, randomUUID } from "node:crypto";
import type { DatabaseClient } from "@steam-top/db";
export type AdminCommandStatus="accepted"|"claimed"|"applied"|"audit_pending"|"completed"|"terminal_failed";
export type AdminCommandOperation=Readonly<{operationId:string;payloadHash:string;action:"platform.pause"|"room.close"|"room.remove";target:string|null;payload:Readonly<Record<string,unknown>>;adminUserId:string;adminSessionId:string;status:AdminCommandStatus;attempts:number;nextRetryAt:Date;leaseToken:string|null;leaseGeneration:number;leaseExpiresAt:Date|null;result:Readonly<Record<string,unknown>>;createdAt:Date;updatedAt:Date}>;
export type AcceptAdminCommand=Pick<AdminCommandOperation,"operationId"|"payloadHash"|"action"|"target"|"payload"|"adminUserId"|"adminSessionId">;
export interface AdminCommandStore{accept(input:AcceptAdminCommand,now?:Date):Promise<{created:boolean;operation:AdminCommandOperation}|{conflict:true}>;get(id:string):Promise<AdminCommandOperation|null>;claimDue(now:Date,leaseMs:number):Promise<AdminCommandOperation|null>;checkpoint(id:string,token:string,status:Exclude<AdminCommandStatus,"claimed">,result?:Readonly<Record<string,unknown>>,nextRetryAt?:Date):Promise<AdminCommandOperation>}
export const adminCommandPayloadHash=(payload:unknown)=>createHash("sha256").update(JSON.stringify(payload)).digest("hex");const terminal=(s:AdminCommandStatus)=>s==="completed"||s==="terminal_failed";
export class InMemoryAdminCommandStore implements AdminCommandStore{readonly operations=new Map<string,AdminCommandOperation>();constructor(private readonly cap=10_000,private readonly ttlMs=86_400_000){}#prune(now:Date){for(const[id,row]of this.operations)if(terminal(row.status)&&now.getTime()-row.updatedAt.getTime()>this.ttlMs)this.operations.delete(id);if(this.operations.size>=this.cap)throw new Error("ADMIN_COMMAND_CAPACITY");}async accept(input:AcceptAdminCommand,now=new Date()){const current=this.operations.get(input.operationId);if(current)return current.payloadHash===input.payloadHash?{created:false,operation:current}:{conflict:true as const};this.#prune(now);const row:AdminCommandOperation={...input,status:"accepted",attempts:0,nextRetryAt:now,leaseToken:null,leaseGeneration:0,leaseExpiresAt:null,result:{status:"accepted"},createdAt:now,updatedAt:now};this.operations.set(input.operationId,row);return{created:true,operation:row};}async get(id:string){return this.operations.get(id)??null;}async claimDue(now:Date,leaseMs:number){for(const[id,row]of this.operations){if(terminal(row.status)||row.nextRetryAt>now||row.status==="claimed"&&row.leaseExpiresAt&&row.leaseExpiresAt>now)continue;const claimed:AdminCommandOperation={...row,status:"claimed",attempts:row.attempts+1,leaseToken:randomUUID(),leaseGeneration:row.leaseGeneration+1,leaseExpiresAt:new Date(now.getTime()+leaseMs),updatedAt:now};this.operations.set(id,claimed);return claimed;}return null;}async checkpoint(id:string,token:string,status:Exclude<AdminCommandStatus,"claimed">,result:Readonly<Record<string,unknown>>={status},nextRetryAt=new Date()){const row=this.operations.get(id);if(!row||row.leaseToken!==token)throw new Error("ADMIN_COMMAND_LEASE_LOST");const next={...row,status,result,nextRetryAt,leaseToken:null,leaseExpiresAt:null,updatedAt:new Date()};this.operations.set(id,next);return next;}}
const mapRow=(r:Record<string,unknown>):AdminCommandOperation=>({operationId:String(r.operation_id),payloadHash:String(r.payload_hash),action:r.action as AdminCommandOperation["action"],target:r.target===null?null:String(r.target),payload:r.payload_json as Record<string,unknown>,adminUserId:String(r.admin_user_id),adminSessionId:String(r.admin_session_id),status:r.status as AdminCommandStatus,attempts:Number(r.attempt_count),nextRetryAt:new Date(String(r.next_retry_at)),leaseToken:r.lease_token===null?null:String(r.lease_token),leaseGeneration:Number(r.lease_generation),leaseExpiresAt:r.lease_expires_at===null?null:new Date(String(r.lease_expires_at)),result:r.result_json as Record<string,unknown>,createdAt:new Date(String(r.created_at)),updatedAt:new Date(String(r.updated_at))});
export class PostgresAdminCommandStore implements AdminCommandStore{constructor(private readonly client:DatabaseClient){}async accept(i:AcceptAdminCommand,now=new Date()){const inserted=await this.client.sql.unsafe("insert into admin_command_operations(operation_id,payload_hash,action,target,payload_json,admin_user_id,admin_session_id,status,result_json,next_retry_at,created_at,updated_at) values($1,$2,$3,$4,$5::jsonb,$6,$7,'accepted','{\"status\":\"accepted\"}'::jsonb,$8,$8,$8) on conflict(operation_id) do nothing returning operation_id",[i.operationId,i.payloadHash,i.action,i.target,JSON.stringify(i.payload),i.adminUserId,i.adminSessionId,now])as readonly Record<string,unknown>[];const row=await this.get(i.operationId);if(!row)throw new Error("ADMIN_COMMAND_ACCEPT_FAILED");return row.payloadHash===i.payloadHash?{created:inserted.length===1,operation:row}:{conflict:true as const};}async get(id:string){const rows=await this.client.sql.unsafe("select * from admin_command_operations where operation_id=$1",[id])as readonly Record<string,unknown>[];return rows[0]?mapRow(rows[0]):null;}async claimDue(now:Date,leaseMs:number){const rows=await this.client.sql.unsafe(`with due as(select operation_id from admin_command_operations where status not in ('completed','terminal_failed') and next_retry_at<=$1 and (status<>'claimed' or lease_expires_at<=$1) order by next_retry_at,created_at for update skip locked limit 1) update admin_command_operations o set status='claimed',attempt_count=attempt_count+1,lease_token=gen_random_uuid(),lease_generation=lease_generation+1,lease_expires_at=$1+($2::text||' milliseconds')::interval,updated_at=$1 from due where o.operation_id=due.operation_id returning o.*`,[now,leaseMs])as readonly Record<string,unknown>[];return rows[0]?mapRow(rows[0]):null;}async checkpoint(id:string,token:string,status:Exclude<AdminCommandStatus,"claimed">,result:Readonly<Record<string,unknown>>={status},nextRetryAt=new Date()){const rows=await this.client.sql.unsafe("update admin_command_operations set status=$3,result_json=$4::jsonb,next_retry_at=$5,lease_token=null,lease_expires_at=null,updated_at=now() where operation_id=$1 and lease_token=$2::uuid returning *",[id,token,status,JSON.stringify(result),nextRetryAt])as readonly Record<string,unknown>[];if(!rows[0])throw new Error("ADMIN_COMMAND_LEASE_LOST");return mapRow(rows[0]);}}
