import { useState } from "react"; import { jsonHeaders, requestJson } from "./api"; import type { Fetcher } from "./types";
type Preview={previewToken:string;filterHash:string;expiresAt:string;counts:{identities:number;designs:number;matches:number}};
export function DeleteDialog({fetcher,csrf,onDeleted,onClose}:{fetcher:Fetcher;csrf:string;onDeleted:()=>Promise<void>;onClose:()=>void}){const[scope,setScope]=useState("all"),[className,setClassName]=useState(""),[password,setPassword]=useState(""),[confirmation,setConfirmation]=useState(""),[preview,setPreview]=useState<Preview|null>(null),[busy,setBusy]=useState(false),[error,setError]=useState("");const filter=scope==="class"?{scope,className}:{scope};return <div className="modal-scrim"><section className="panel delete-dialog" role="dialog" aria-modal="true" aria-labelledby="delete-title"><h2 id="delete-title">刪除紀錄</h2><p className="field-error">此操作不可還原，並會留下不可修改的稽核紀錄。</p><label>刪除範圍<select value={scope} onChange={e=>{setScope(e.target.value);setPreview(null);}}><option value="all">全部紀錄</option><option value="class">指定班別</option></select></label>{scope==="class"?<label>班別<input value={className} onChange={e=>setClassName(e.target.value)}/></label>:null}<button onClick={async()=>{setBusy(true);setError("");try{setPreview(await requestJson(fetcher,"/api/admin/records/deletion-preview",{method:"POST",headers:jsonHeaders(csrf),body:JSON.stringify(filter)}));}catch{setError("未能建立刪除預覽。");}finally{setBusy(false);}}} disabled={busy||scope==="class"&&!className}>預覽刪除範圍</button>{preview?<><p role="status">將刪除 {preview.counts.identities} 個身份、{preview.counts.designs} 個設計、{preview.counts.matches} 場對戰。</p><label>再次輸入管理員密碼<input type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></label><label>輸入 DELETE 確認<input value={confirmation} onChange={e=>setConfirmation(e.target.value)}/></label><button className="danger-button" disabled={busy||password.length<8||confirmation!=="DELETE"} onClick={async()=>{setBusy(true);setError("");try{await requestJson(fetcher,"/api/admin/records",{method:"DELETE",headers:jsonHeaders(csrf),body:JSON.stringify({previewToken:preview.previewToken,filterHash:preview.filterHash,password,confirmation})});setPassword("");setConfirmation("");setPreview(null);await onDeleted();onClose();}catch{setPassword("");setPreview(null);setError("刪除失敗；請重新預覽。");}finally{setBusy(false);}}}>永久刪除</button></>:null}{error?<p role="alert" className="field-error">{error}</p>:null}<button onClick={onClose}>取消</button></section></div>}
