import { Bar, BarChart, CartesianGrid, Legend, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts"; import type { AnalyticsResponse } from "./types";
export function AnalyticsCharts({ data }: { data: AnalyticsResponse }) { const eligible = data.rankings.top.filter(row => Number(row.sampleSize ?? 0) >= 10); return <section className="panel admin-section" aria-labelledby="analytics-title"><h2 id="analytics-title">使用及參數統計</h2><div className="chart-grid">{(["daily","weekly","monthly"] as const).map(period => <figure key={period}><figcaption>{period === "daily" ? "每日" : period === "weekly" ? "每週" : "每月"}使用量</figcaption><div className="chart-frame"><ResponsiveContainer width="100%" height="100%"><BarChart data={data.usagePeriods[period]}><CartesianGrid strokeDasharray="3 3"/><XAxis dataKey="period"/><YAxis/><Tooltip/><Legend/><Bar dataKey="activeDevices" name="使用裝置" fill="#2858a5"/><Bar dataKey="matches" name="對戰" fill="#43a875"/></BarChart></ResponsiveContainer></div></figure>)}</div><h3>全部參數使用比例</h3><div className="table-scroll"><table><thead><tr><th>參數類別</th><th>參數</th><th>次數</th><th>比例</th></tr></thead><tbody>{data.parameterUsage.map((row,index) => <tr key={`${row.dimension}-${row.value}-${index}`}><td>{row.dimension}</td><td>{row.value}</td><td>{row.count}</td><td>{Math.round(Number(row.ratio) * 100)}%</td></tr>)}</tbody></table></div><h3>目前最高平均表現參數（樣本最少 10 場）</h3>{eligible.length ? <ul>{eligible.map((row,index) => <li key={index}><strong>{row.label}</strong>　平均分 {row.averageScore}　勝率 {Math.round(Number(row.winRate) * 100)}%　樣本 {row.sampleSize}</li>)}</ul> : <p>尚未有足夠樣本。</p>}</section>; }
