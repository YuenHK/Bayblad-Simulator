services:
  server:
    environment:
      DATABASE_URL_FILE: /run/secrets/steam_top_app_database_url
    secrets:
      - steam_top_app_database_url
secrets:
  steam_top_app_database_url: { environment: APP_DATABASE_URL }
