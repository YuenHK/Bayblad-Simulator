import {execFileSync} from "node:child_process";import {createHash} from "node:crypto";import {chmodSync,copyFileSync,lstatSync,mkdtempSync,readFileSync,realpathSync,rmSync,symlinkSync,writeFileSync} from "node:fs";import {tmpdir} from "node:os";import {join} from "node:path";import {afterEach,expect,it} from "vitest";
const roots:string[]=[];afterEach(()=>{while(roots.length)rmSync(roots.pop()!,{recursive:true,force:true})});const sha=(p:string)=>createHash("sha256").update(readFileSync(p)).digest("hex");
const fixture=()=>{const root=mkdtempSync(join(realpathSync(tmpdir()),"policy-installed-"));roots.push(root);const signer=join(root,"signer.py"),manifest=join(root,"manifest.json"),runtime=realpathSync("/usr/bin/python3");copyFileSync("scripts/sign-production-policy-entry.py",signer);chmodSync(signer,0o444);const s=lstatSync(runtime,{bigint:true}),value={schemaVersion:1,purpose:"steam-top-production-policy-signer",signerPath:signer,signerSha256:sha(signer),pythonPath:runtime,pythonSha256:sha(runtime),pythonStat:{dev:Number(s.dev),ino:Number(s.ino),size:Number(s.size),mtimeNs:Number(s.mtimeNs),ctimeNs:Number(s.ctimeNs),uid:Number(s.uid),mode:Number(s.mode&0o777n),nlink:Number(s.nlink)}};writeFileSync(manifest,`${JSON.stringify(value)}\n`,{mode:0o400});return{root,signer,manifest,value}};
const verify=(manifest:string)=>execFileSync("/usr/bin/python3",["-I","-E","-S","infra/bootstrap/invoke-production-policy-signer.py","--test-manifest",manifest],{stdio:"pipe"});
it("rejects tampered installed signer and runtime digest",()=>{let x=fixture();chmodSync(x.signer,0o644);writeFileSync(x.signer,"# tampered\n",{mode:0o444});expect(()=>verify(x.manifest)).toThrow();x=fixture();chmodSync(x.manifest,0o600);writeFileSync(x.manifest,`${JSON.stringify({...x.value,pythonSha256:"0".repeat(64)})}\n`);chmodSync(x.manifest,0o400);expect(()=>verify(x.manifest)).toThrow()});
it("rejects noncanonical writable and symlink manifests",()=>{let x=fixture();chmodSync(x.manifest,0o600);expect(()=>verify(x.manifest)).toThrow();x=fixture();const link=join(x.root,"manifest-link.json");symlinkSync(x.manifest,link);expect(()=>verify(link)).toThrow();x=fixture();chmodSync(x.manifest,0o600);writeFileSync(x.manifest,`${JSON.stringify({...x.value,extra:true})}\n`);chmodSync(x.manifest,0o400);expect(()=>verify(x.manifest)).toThrow()});
it("rejects writable installed signer mode",()=>{const x=fixture();chmodSync(x.signer,0o666);expect(()=>verify(x.manifest)).toThrow()});
