import{execFile}from"node:child_process";import{mkdtemp,readFile,writeFile}from"node:fs/promises";import{tmpdir}from"node:os";import{join}from"node:path";import{promisify}from"node:util";import{expect,it}from"vitest";const run=promisify(execFile);
it("creates exact GitHub deployment request JSON",async()=>{const d=await mkdtemp(join(tmpdir(),"deploy-request-")),m=join(d,"m"),o=join(d,"o"),r=join(d,"r"),c="a".repeat(40);await writeFile(m,JSON.stringify({commit:c,images:{server:"s",migration:"s",web:"w",database:"d"}}));await run("node",["scripts/create-deployment-request.mjs",m,"42",`release-manifest-${c}`,"b".repeat(64),c,"c".repeat(64),o,r]);const x=JSON.parse(await readFile(o,"utf8"));expect(x).toMatchObject({ref:c,environment:"production",required_contexts:[],auto_merge:false,payload:{schemaVersion:1,smokeResult:"passed",databaseImage:"d"}});expect(Object.keys(x).sort()).toEqual(["auto_merge","environment","payload","ref","required_contexts"]);});
